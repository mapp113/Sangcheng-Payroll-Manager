spring:
  application:
    name: Sangcheng Payroll Manager

  datasource:
    url: jdbc:mysql://localhost:3306/hrm?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:ngolhhe163179@fpt.edu.vn}
    password: ${SPRING_MAIL_PASSWORD:lupq yctf rewq csxu}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}

jwt:
  secret: cd1a3133c39ce3eadab3bc8089ec59f7242afa3ffcc890ece7c4f66a15ada29b
  expiration: 3600000

zkteco:
  enabled: true
  ip: 192.168.0.2
  port: 4370
  machine-no: 1
  # Password giao tiếp SDK nếu có, nếu không thì để rỗng
  password:

app:
  password-reset:
    code-length: ${APP_PASSWORD_RESET_CODE_LENGTH:6}
    expiry-minutes: ${APP_PASSWORD_RESET_EXPIRY_MINUTES:10}
    mail:
      from: ${APP_PASSWORD_RESET_MAIL_FROM:noreply@sangcheng-payroll.com}
      enabled: ${APP_PASSWORD_RESET_MAIL_ENABLED:true}